{
	"name": "ccantynz-alt/intelligent-plugin-conflict-detector",
	"description": "WordPress plugin that intelligently detects plugin conflicts, tests in a safe environment, alerts users, and provides one-click rollback.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=7.4"
	},
	"require-dev": {
		"phpunit/phpunit": "^9.6",
		"brain/monkey": "^2.6",
		"mockery/mockery": "^1.5"
	},
	"autoload-dev": {
		"psr-4": {
			"IPCD\\Tests\\": "tests/"
		}
	},
	"scripts": {
		"test": "phpunit"
	},
	"config": {
		"optimize-autoloader": true,
		"sort-packages": true,
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	}
}
